public class Maths{
    
    public static void main(String[] args){
        System.out.println("oiiii");

        double raiz = Math.sqrt(25);
        int maior = Math.max(10,20);
        int menor = Math.min(50,45);
        double potencia = Math.pow(2,4);
        double absoluto = Math.abs(90.997);
        double arredonda = Math.round(32.667);
        int numRDM = (int)(Math.random()*101);

        System.out.println(+numRDM);
    }
}